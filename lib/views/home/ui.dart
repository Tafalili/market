import 'package:flutter/material.dart';

class Nav_Home extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: SafeArea(child:Center(child: Text("nav home"),)),
    );
  }
}
