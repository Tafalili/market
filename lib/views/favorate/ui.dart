import 'package:flutter/material.dart';

class Nav_Favorate extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: SafeArea(child:Center(child: Text("nav Favorate"),)),
    );
  }
}
